import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 30,
    color: '#E5BF3C'
  },
  image: {
    maxWidth:180, 
    maxHeight: 111,
    alignSelf:'center',
    resizeMode: 'contain',
  },
});
